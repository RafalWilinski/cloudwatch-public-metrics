service: lambda-dashboard

provider:
  name: aws
  runtime: nodejs6.10
  environment: ${file(./config.yml)}

plugins:
  - serverless-offline

functions:
  main:
    handler: lambda.handler
    events:
      - http:
          path: /
          method: ANY
      - http:
          path: /{any+}
          method: ANY
